name: Trigger modrex-site rebuild

on:
    release:
        types: [published]

jobs:
    trigger:
        runs-on: ubuntu-latest
        environment: release-delay
        steps:
            - name: Call Cloudflare Pages deploy hook
              run: curl -fsS -X POST "${{ secrets.CLOUDFLARE_SITE_DEPLOY_HOOK_URL }}"
