Add user-facing entries to CHANGELOG.md's Unreleased section for recent commits or uncommitted changes. Pass a ref or range (e.g. HEAD~3..HEAD) to scope it, or nothing to reconcile against the last tag.

Load and follow the full instructions from .agents/skills/changelog/SKILL.md with argument: $ARGUMENTS
