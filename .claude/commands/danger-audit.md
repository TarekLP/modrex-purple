Audit a diff for the five dangerous AI patterns (silent catch, generic helpers, defensive checks, deep nesting, fake abstractions). Pass a path, branch, or file to audit that target, or nothing to audit `git diff HEAD`.

Load and follow the full instructions from .agents/skills/danger-audit/SKILL.md with argument: $ARGUMENTS
